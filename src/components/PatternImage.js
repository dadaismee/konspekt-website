import React from "react";
import styled from "styled-components";

const PatternImage = () => {
  return <Wrapper src="/images/graphics/pattern_bar.svg" />;
};

export default PatternImage;

const Wrapper = styled.img`
  width: 100%;
  height: 100%;
`;
